# W02I: Replace console-derived privileged IPC identity

Canonical SHA-256: `3acdebbd81396e7804d432127141c9890fbf3d665bc1d41e84f267e93a3b9f9a`

- Status: `parked`
- Controller: `agent`
- Type: `work.implementation`
- Depends on: `V02`
- Obligations: `O03`, `O04`
- Oracle class: source-authenticated IPC and native adversarial origin behavior
- Evidence class: focused tests and native positive/negative capability receipts
- Implementation mode: `real`
- Live oracle required: `true`
- Substitution allowed: `false`

## Gist

Bind each privileged call to its engine-authenticated initiating document, including remote frames and navigation changes.

## Park and resume condition

Native Windows run 33334815996 reproduces mapped-asset cross-origin disclosure and CSP bypass. Published Servo 0.5.0 cannot provide both the required origin semantics and normal app fetch/modules through its documented embedding paths. No safe embedder-only repair has been established.

Resume: A public Servo release or accepted public API revision supplies standards-preserving asset transport/origins, or the user explicitly changes the published-engine constraint to authorize an isolated patched-engine integration lane; rerun the unchanged native positive/negative suite before discharge.

## Scope

- Turvo IPC transport and dependency manifests, initialization-script frame scope, source-identity tests, example CSP configuration, examples/security, native security CI and its observed graphics/shutdown blockers, public Servo API research or isolated patch, and scoped README/AGENTS security status

## Consumes

- V02 asset routing
- F08
- ADR04
- ADR05

## Produces

- source-authenticated IPC adapter
- main-frame initialization handling
- native origin regression probes

## Blueprint

- Research and reuse the pinned engine request surface before introducing a transport; never trust a page-supplied origin, top-level URL substitution, or a hardcoded Origin header.
- Preserve payloads, callbacks, binary responses, and channel fetches; honor Tauri main-frame initialization flags.
- Require real local success, remote top-level denial, remote iframe denial, opaque-origin denial, and navigation-race denial. If the published engine lacks a sufficient API, produce a public compatibility proposal and retain an explicit release blocker.

## Proof commands

- `cargo test -p turvo --lib --tests --locked`
- `native IPC source-authentication suite`

## Discharge evidence

Not yet discharged.

## Partial receipts (not discharge)

- F17/F25: all nine native IPC/security cases pass on Linux and macOS, with exact native command/event payloads and clean shutdown.
- F19/F25: Windows ANGLE startup and ordinary JSON/raw/binary/channel IPC run, but remote asset disclosure and CSP handler dispatch fail the fixed native security oracle.
- The public API alternatives and repair ladder are recorded in docs/research/protocol-origin-boundary.md; no private-fork requirement has been adopted.

## Non-conclusions

- Unix fixture success is not normal custom-origin fetch/module compatibility or full production security proof.
- Windows compilation and startup are not native security acceptance.

## Retraction path

Revert the transport adapter and keep privileged IPC release-blocked; do not reinstate an untrusted identity fallback.
