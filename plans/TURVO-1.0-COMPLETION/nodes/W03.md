# W03: Create a runtime-neutral upstream new-window seam

Canonical SHA-256: `3acdebbd81396e7804d432127141c9890fbf3d665bc1d41e84f267e93a3b9f9a`

- Status: `parked`
- Controller: `agent`
- Type: `work.external`
- Depends on: `V02`
- Obligations: `O06`
- Oracle class: upstream compile, tests, and public review artifact
- Evidence class: upstream-source test receipt and public PR URL or versioned proposal commit
- Implementation mode: `real_external_patch`
- Live oracle required: `true`
- Substitution allowed: `false`

## Gist

Implement and propose an additive Tauri API that lets non-Wry runtimes process window.open safely.

## Park and resume condition

The versioned Tauri proposal passed all three compatibility jobs (F24), but the complete V03 Servo-sufficiency gate is not established: the pinned engine request lacks target URL/geometry provenance (F23), and no accepted public integration revision or native Servo popup receipt exists. Partial API proof is retained without discharging O06.

Resume: An accepted public engine API or validated design supplies the real popup metadata and responder lifecycle, or the user explicitly authorizes a patched-engine development lane. Re-enter V03 verification before adopting a public Tauri revision; do not invent popup metadata.

## Scope

- external Tauri clean worktree, patches/tauri, docs/research/tauri-new-window.md, and a scoped upstream-patch validation workflow

## Consumes

- ADR02
- V02 baseline

## Produces

- upstream tests
- public Tauri PR or versioned patch
- Turvo integration note

## Blueprint

- Preserve Wry behavior and public compatibility.
- Give runtime-neutral embedders enough request information to create or deny a new window without native Wry objects.

## Proof commands

- `cargo test -p tauri-runtime --lib --locked`
- `cargo check -p tauri --no-default-features --lib --locked`
- `cargo test -p tauri --lib --tests --features test --locked`

## Discharge evidence

Not yet discharged.

## Partial receipts (not discharge)

- Public proposal commit d4c8b2bba0c6213e2e7e3c7638dc9da2c9321994 contains the exact pinned Tauri patch and integration note; bc4946982bfa385a39f25a62bbaa7b5340a1d497 fixes only the hosted checkout line-ending issue.
- Run 33336218434 passed all three declared proof commands on Linux, macOS, and Windows; F24 names the jobs and test counts.
- A separate read-only source pass checked native constructor/accessor compatibility, default rejection, private builder storage, and the actual Servo creation request. It found F23, so full Servo-adapter sufficiency is not claimed; no independent-agent or maintainer approval is represented.

## Non-conclusions

- No Servo window.open integration, upstream approval, MSRV/mobile matrix, or native Wry popup parity is established by these compile/mock tests.
- Turvo still resolves the published Tauri and Servo crates; the proposal is not silently adopted.

## Retraction path

Close the PR or revise the versioned proposal and retain Turvo's documented blocked behavior if the design is rejected.
