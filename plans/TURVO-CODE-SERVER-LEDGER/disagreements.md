# Decisions and disagreements

Canonical SHA-256: `ab206455c67a6dcc18d7d9af4ac802cf14b2465caf626cc5d64e7e3f88b0dcc2`

## D01: Default routing owner

Choice: assign untriaged native console symptoms to the Turvo integration owner.

Reversibility: `reversible`.

Retraction: add a fingerprint-locked entry to `ownership.json` after native diagnosis routes the class to `servo-fork` or `code-server-fork`.

## Current disagreement rows

None. Ownership overrides are routing decisions, not claims that a source file location proves root cause.

Ledger state at render: `capture_pending`.
