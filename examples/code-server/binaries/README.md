# Sidecar binaries

Place the code-server hard-fork executable here as
`code-server-<target-triple>`. Tauri selects the target-suffixed binary from the
`binaries/code-server` external-binary declaration; binaries are build outputs
and are not committed to this repository.
